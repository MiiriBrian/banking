'use server';

export const signIn = async () => {
    try{
        //Mutation ?DB/ fetch?
    }
    catch(error) {
        console.error('error')

    }
}
export const signUp = async (userData: SignUpParams) => {
    try{
        //Create user account
    }
    catch(error) {
        console.error('error')

    }
}